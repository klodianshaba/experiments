import { ForeachDirective } from './foreach.directive';

describe('ForeachDirective', () => {
  it('should create an instance', () => {
    const directive = new ForeachDirective();
    expect(directive).toBeTruthy();
  });
});
